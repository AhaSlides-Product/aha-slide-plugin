/** Default BroadcastChannel name carrying the completion ping. */
export const DEFAULT_CHANNEL = 'aha-auth';

/** The only message this package ever sends. It carries NO payload by design. */
export const DONE_MESSAGE = 'aha-auth:done';

/** Default `window.open` target name. */
export const DEFAULT_WINDOW_NAME = 'aha-auth';

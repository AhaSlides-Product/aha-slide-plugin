{
  "name": "@aha/auth",
  "version": "0.2.0",
  "description": "Framework-agnostic embedded sign-in orchestration for AhaSlides apps — frames the platform login, learns when it finished, and never touches a credential.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AhaSlides-Product/aha-slide-plugin.git",
    "directory": "packages/auth"
  },
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "tsc -p tsconfig.json --noEmit && vitest run",
    "test:watch": "vitest watch",
    "clean": "rm -rf dist *.tsbuildinfo",
    "docs": "typedoc --skipErrorChecking --plugin typedoc-plugin-markdown --out docs src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}

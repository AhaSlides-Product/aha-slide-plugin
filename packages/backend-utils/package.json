{
  "name": "@aha/backend-utils",
  "version": "1.0.0",
  "description": "Shared backend utilities and DTOs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist *.tsbuildinfo",
    "docs": "typedoc --skipErrorChecking --plugin typedoc-plugin-markdown --out docs src/index.ts"
  },
  "dependencies": {
    "@aha/common": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
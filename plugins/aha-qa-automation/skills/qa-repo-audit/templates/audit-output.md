# QA Audit — <repo-name>

> Generated by `qa-repo-audit` on <date>. Companion to `.qa-profile.json`, which is the
> machine-readable version of section 2. Re-run the skill after any restructure of the
> test layer.

## 1. Summary

| | |
|---|---|
| Archetype | `standalone-qa` \| `product-repo` |
| Test root | `<testRoot>` |
| Language | `ts` \| `js` |
| Playwright specs | `<n>` files / `<n>` tests |
| Lower-layer suites | `<n>` (`vitest`, `pytest`, …) or **none** |
| Case-id convention | `<pattern>` — enforced / not enforced |

## 2. Layer inventory

| Layer | Directory | Spec files | Tests | Notes |
|---|---|---|---|---|
| api | `specs/api` | 18 | 142 | |
| e2e | `specs/e2e` | 9 | 114 | |

### Lower layers (read-only inventory)

| Kind | Tool | Root | Command | Test files |
|---|---|---|---|---|
| unit | vitest | `frontend` | `pnpm test:unit` | 46 |
| integration | pytest | `backend` | `pytest tests/integration` | 31 |

> This inventory exists so `qa-design-cases` can push a case DOWN to a layer that already
> exists instead of proving it through the browser. Nothing here is executed or judged by
> the audit.
>
> **If this table is empty**, the repo owns no lower layer. Cases that belong at a lower
> layer become *gap recommendations* in the design — they must not be promoted to E2E.

## 3. Conventions adopted

Precedence order, general → specific. These outrank this plugin's defaults for this repo.

1. `CLAUDE.md`
2. `tests/CLAUDE.md`
3. `tests/TEST_SCRIPT_RULES.md`

## 4. Rules inferred, with evidence

| Rule | Value | Evidence |
|---|---|---|
| `noRawLocatorsInSpecs` | `true` | 0 hits of `page.getBy*` across 27 spec files |
| `locatorGetterPrefix` | `get` | 61/63 Locator-returning methods start with `get` |
| `noExpectInPageObjects` | `true` | stated in `TEST_SCRIPT_RULES.md` §2.4 |
| `requireCaseIdInTitle` | `true` | 251/256 titles carry an id |

> A rule is set `true` only when the repo already satisfies it. Turning on a rule the repo
> violates in bulk makes stage 5's linter permanently red, and a permanently red linter
> gets switched off.

## 5. Gaps and findings

- **Competing test locations** — `<path>` also contains specs but does not follow
  `<conventions doc>`. Recommend: deprecate / migrate / adopt.
- **Untraceable tests** — `<n>` tests carry no case id, so design↔code coverage cannot be
  checked for them.
- **Uncovered surfaces** — product areas with no test at any layer.

## 6. Validation

| Gate | Verdict |
|---|---|
| `qa-profile-validate.mjs --run-commands` | PASS / FAIL |
| `qa-audit-verifier` | PASS / FAIL |
| Human confirmation | confirmed by `<name>` on `<date>` |
